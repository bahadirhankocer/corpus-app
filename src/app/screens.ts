export type ScreenId = 'corpus' | 'feed' | 'map';

export const SCREENS: ScreenId[] = ['corpus', 'feed', 'map'];

export type LayerId = 'sequence' | 'links' | 'digests' | 'audiolog' | 'settings' | 'versions';

export const LAYERS: LayerId[] = ['sequence', 'links', 'digests', 'audiolog', 'settings', 'versions'];
